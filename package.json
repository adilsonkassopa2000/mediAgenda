{
  "name": "consultorio-front",
  "version": "1.0.0",
  "description": "Plataforma MediAgenda - Consultas Externas Online",
  "scripts": {
    "build:css": "tailwindcss -i ./styles.input.css -o ./styles.css --minify",
    "watch:css": "tailwindcss -i ./styles.input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  }
}
